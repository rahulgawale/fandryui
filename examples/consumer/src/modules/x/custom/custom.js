import Base from 'fandry/base';

// A consumer-owned component built on Fandry's shared base class: it gets the
// same stylesheet and design tokens with no other Fandry component involved.
export default class Custom extends Base {}
